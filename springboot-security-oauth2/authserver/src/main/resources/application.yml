spring:
  datasource:
    hikari:
      connection-test-query: SELECT 1
      minimum-idle: 1
      maximum-pool-size: 5
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql:3306/platform_base?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&useSSL=false
    username: root
    password: Root123456@
    initialization-mode: always
  mvc:
    servlet:
      load-on-startup: 1
server:
  port: 8089
security:
  oauth2:
    authorization:
      check-token-access: true

